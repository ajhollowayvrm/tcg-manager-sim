# tcg-manager-sim

Emptied. Every directory has been removed; what remains is the root-level
configuration — `index.html`, `package.json`, `package-lock.json`,
`vite.config.js` and `.gitignore`.

There is no application code, no iOS shell, no static assets and no CI
workflow. `npm run build` produces an empty page.

The full history is preserved in git, so everything removed can be recovered
from an earlier commit.
