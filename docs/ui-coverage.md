# UI coverage

Generated by `harness/coverage.ts`. Do not edit by hand.

| Check | Result |
|---|---|
| `api.*` decisions | 21 |
| ...with a control in `src/app` | 21 |
| `Decision` kinds with no wrapper | 0 |
| Config leaves a decision selects among | 271 |
| Config leaves shown as a readout | 58 |
| Config leaves that are balance constants | 331 |
| Config leaves unclassified | 0 |

## How each config block is reached

| Block or leaf | How | By |
|---|---|---|
| `printing.unitCost` | decision | commitPrintRun(quality) — the tier menu |
| `printing.errorRate` | decision | commitPrintRun(quality) |
| `printing.qualityGradeShift` | decision | commitPrintRun(quality) |
| `rarity.pull` | decision | designCard(rarity) + createSet(targetSize) — the rarity ladder |
| `rarity.weight` | decision | designCard(rarity) |
| `unlocks` | decision | purchaseUnlock — the growth shop |
| `unlocks.enforcePrintQuality` | constant | Feature flag on the restriction, not the restriction. |
| `unlocks.enforceSpecialtySlots` | constant | Feature flag on the restriction, not the restriction. |
| `readings` | decision | purchaseUnlock — research, community, analytics |
| `readings.rereadWeeks` | constant | A reading holds a quarter: shorter flickers, longer lets a player wait out the error. |
| `readings.forecastDriftWindowWeeks` | constant | How a forecast measures its trend. |
| `readings.forecastMaxDriftPerYear` | constant | Bound on extrapolation. Nothing in the value engine reads it. |
| `readings.forecastHorizonWeeks` | constant | How fast a forecast widens with distance. |
| `collabs.advanceMin` | decision | signCollab — money now against a share of the set |
| `collabs.advanceMax` | decision | signCollab |
| `collabs.royaltyShareMin` | decision | signCollab |
| `collabs.royaltyShareMax` | decision | signCollab |
| `collabs.minimumGuaranteeMultiple` | decision | signCollab — what a flop still owes |
| `collabs.reachBonusMin` | decision | signCollab |
| `collabs.reachBonusMax` | decision | signCollab |
| `collabs.gateMin` | decision | signCollab — the standing an offer demands |
| `collabs.gateMax` | decision | signCollab |
| `collabs.segmentsReachedMin` | decision | signCollab — which offer reaches whom |
| `collabs.segmentsReachedMax` | decision | signCollab |
| `collabs.offerWindowWeeks` | decision | signCollab — how long you may think about it |
| `collabs.exposureShare` | decision | signCollab — the rent a licensor charges your own IP |
| `events` | decision | hostEvent(scale, budget) |
| `chains` | decision | designCard(progressionLink, illustrationLink) |
| `chains.subjectReference` | constant | Normaliser for the weak-subject hedge. |
| `preorders` | decision | openPreorders(unitsCap) |
| `preorders.goodwillPerUnfilled` | decision | openPreorders — the cost of a broken promise |
| `channels.traits` | decision | allocate — the terms you pick a channel for |
| `channels.seeds` | decision | allocate + purchaseUnlock(channels) |
| `channels.unlockCost` | decision | purchaseUnlock(channels) |
| `world.regions` | decision | unlockRegion — the region menu |
| `world.productPreference` | decision | defineProduct(kind) — the nine template forms; a studio-invented one rolls its own |
| `world.customLinePreferenceMin` | decision | defineProduct(kind) — the span a studio-invented form is rolled from |
| `world.customLinePreferenceMax` | decision | defineProduct(kind) — the span a studio-invented form is rolled from |
| `hype.defaultLeadWeeks` | decision | scheduleReveal(startTick) |
| `hype.defaultCadenceWeeks` | decision | scheduleReveal(cadence) |
| `hype.marketingReference` | decision | marketingSpend — where the log curve bends |
| `hype.marketingHypeGain` | decision | marketingSpend |
| `hype.prereleaseCostPerScale` | decision | hostPrerelease(scale, budget) |
| `hype.prereleaseHypeGain` | decision | hostPrerelease |
| `hype.prereleaseGoodwillGain` | decision | hostPrerelease |
| `hype.prereleaseRelationshipGain` | decision | hostPrerelease |
| `drops.cadenceWeeks` | decision | scheduleDrop(atTick) |
| `drops.holdLimitWeeks` | decision | scheduleDrop — how long a scalper holds |
| `art.openingRateMin` | decision | commissionArt — the rate you pay |
| `art.openingRateMax` | decision | commissionArt |
| `art.newcomerRateMin` | decision | commissionArt |
| `art.newcomerRateMax` | decision | commissionArt |
| `art.retainerWeeklyMultiple` | decision | hireArtist(retainer) |
| `art.exclusiveWeeklyMultiple` | decision | hireArtist(exclusive) |
| `art.retainerFeeDiscount` | decision | hireArtist(retainer) |
| `art.exclusiveFeeDiscount` | decision | hireArtist(exclusive) |
| `art.budgetQualityGain` | decision | commissionArt(brief.budget) — the return on paying over |
| `art.minRelationshipToAccept` | decision | commissionArt — whether they take the call |
| `art.brandStandingOffsetsRelationship` | decision | commissionArt |
| `finance.startingCash` | decision | the opening position, overridden for play in store.ts |
| `finance.interestBase` | decision | borrow — the rate |
| `finance.creditToRate` | decision | borrow / repay — what credit buys off the rate |
| `finance.borrowCeilingBase` | decision | borrow — the ceiling |
| `finance.borrowCeilingMultiple` | decision | borrow |
| `finance.weeklyOverheadPerChannel` | decision | purchaseUnlock(channels) — reach costs money to run |
| `finance.weeklyOverheadPerRegion` | decision | unlockRegion — an office abroad is a standing cost |
| `finance.storagePerUnitPerTick` | decision | commitPrintRun(quantities) — what unsold stock costs |
| `finance.storageSurchargeAfterTicks` | decision | commitPrintRun — the overprint cliff |
| `finance.storageSurchargeMultiple` | decision | commitPrintRun |
| `region.entryLeadWeeks` | decision | unlockRegion — 26 weeks before it ships anything |
| `region.mismatchPenalty` | decision | defineProduct(region) — the cost of being wrong |
| `region.knowledgeGainPerResearch` | decision | purchaseUnlock(marketResearch) |
| `region.researchCreditShare` | decision | purchaseUnlock(marketResearch) |
| `treatments` | decision | designCard(treatments) — the finish picker |
| `desire` | decision | designCard — how a card connects |
| `archetypes` | decision | createIp(archetype) — the prior a character is rolled from |
| `graders` | readout | the pop report. Grading is the market acting, not a decision the studio makes — the studio reaches it through print quality. |
| `grading.tierMultiplier` | readout | the pop report — the payoff ladder |
| `attention` | readout | the ruler print size and release cadence are measured against. Shown, never set. |
| `finance.brandBase` | readout | brand standing, which gates channels, collabs and artists |
| `finance.brandFromAffection` | readout | brand standing |
| `finance.brandFromGoodwill` | readout | brand standing |
| `value` | constant | The price stack, tuned as ONE unit over 30 seeds x 25 years. Moving one knob and re-measuring one metric looks fine and is wrong. |
| `affection` | constant | How an audience bonds. The player chooses WHO is on the card, never how bonding works. |
| `sealed` | constant | The sealed price stack, coupled to singles but not derived from them. |
| `actors` | constant | Populations, not choices. |
| `creators` | constant | You cannot pay a creator or brief one. That IS the mechanism. |
| `audience` | constant | Demography. Never swept. |
| `market` | constant | The market climate walk. |
| `history` | constant | Retention and compaction. A storage decision, not a game one. |
| `grading` | constant | The latent condition scale and its cuts. |
| `strides` | constant | How often each subsystem runs. Moving one renumbers every later RNG draw and invalidates every banked gate. |
| `startYear` | constant | Where the calendar starts. |
| `printing` | constant | Cost and misprint incidence outside the tier menu. |
| `rarity` | constant | Divisors and the set-size reference that scale the pull table. |
| `channels` | constant | Souring, capacity drift and the evaluation window — how a relationship decays, which the player causes but does not set. |
| `world` | constant | World bootstrap: segment sizes and opening actor populations. |
| `hype` | constant | How hype decays and how noisy the signal is. |
| `drops` | constant | The scalper population loop. |
| `art` | constant | Artist stats, growth (hidden on purpose), turnaround and the roster churn. |
| `finance` | constant | Credit drift, the brand target and the overhead scaling. |
| `region` | constant | Region fit weights and the reading noise model. |
| `collabs` | constant | How often offers arrive and how their reach converts. |
